# WSL-MCP-Server
Secure Model Context Protocol (MCP) server that runs a tightly controlled set of Linux commands through WSL.  This server is designed for AI-assisted security workflows where command execution is useful, but data leakage and arbitrary code execution must be aggressively blocked.
